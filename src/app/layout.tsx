import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "../components/Navbar";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Short Video Search Engine | Find YouTube, TikTok, Instagram & Facebook Videos",
  description: "Search short-form videos across YouTube, TikTok, Instagram and Facebook from one simple research tool.",
  openGraph: {
    title: "Short Video Search Engine",
    description: "Search short-form videos across the web",
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-gray-50 dark:bg-gray-950 text-gray-900 dark:text-gray-100 min-h-screen flex flex-col`}>
        <Navbar />
        <main className="flex-grow">
          {children}
        </main>
      </body>
    </html>
  );
}
